# Core runtime
rich>=13.7.0
requests>=2.31.0
flask>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0
eventlet>=0.35.2
wafw00f>=2.2.0

# PDF export — weasyprint needs system libs (libpango, libcairo).
# In CI / headless environments it is installed but PDF export is skipped
# gracefully when the system libs are absent.
weasyprint>=60.0

# Testing
pytest>=8.0.0
hypothesis>=6.100.0
